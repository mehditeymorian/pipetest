base "https://api.example.com"

let group_id = "g1"

req login:
  POST /auth/login
  json { email: env("EMAIL") }
  let token = "t1"

req listOrders:
  GET /groups/:group_id/orders
  auth bearer token

flow "happy":
  login -> listOrders : orders

  ? orders.status == 200
